<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <title>Queen Attack!</title>
  </head>
  <body>
    <div class="container">
      <h1>Queen Attack</h1>

      <p>Enter X and Y coordinates for a chess queen and her potential target piece to see if she can attack the piece</p>
      <form action="/result" method="get">
        <div class="form-group">
          <label for="queen-x">Queen Coordinates</label>
          <input type="text" name="queen-x" placeholder="X Coordinate">
          <input type="text" name="queen-y" placeholder="Y Coordinate">
        </div>
        <div class="form-group">
          <label for="target-x">Target Coordinates</label>
          <input type="text" name="target-x" placeholder="X Coordinate">
          <input type="text" name="target-y" placeholder="Y Coordinate">
        </div>
        <button type="submit">Submit</button>
      </form>
      @if (@Model != null )
      {
        @if (@Model == true)
        {
          <p>The Queen can attack!</p>
        }
        else
        {
          <p>The Queen cannot attack :-( </p>
        }
      }
    </div>
  </body>
</html>
